{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/:path((?!assets/|favicon\\.ico|robots\\.txt|manifest\\.webmanifest).*)",
      "destination": "/index.html"
    }
  ]
}
